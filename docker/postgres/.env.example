# Copy this file to docker/postgres/.env and fill in real values before
# deploying this stack. docker/postgres/.env is gitignored - never commit the
# real credentials.
POSTGRES_USER=pyhss
POSTGRES_PASSWORD=changeThisToAStrongPassword
POSTGRES_DB=hss
